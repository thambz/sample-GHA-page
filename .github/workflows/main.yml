# ============================================================
# FASTAPI — PREMIUM PRODUCTION CI/CD PIPELINE
# Compact Enterprise Dashboard Edition
# ============================================================

name: FastAPI Production CI/CD

on:

  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

env:
  PYTHON_VERSION: "3.11"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

# ============================================================
# TEST
# ============================================================

  test:

    name: Test & Coverage

    runs-on: ubuntu-latest

    outputs:
      coverage: ${{ steps.coverage.outputs.pct }}

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-html

      - name: Run tests
        run: |

          mkdir -p reports

          pytest \
            --junitxml=reports/junit.xml \
            --cov=app \
            --cov-report=xml:reports/coverage.xml \
            --cov-report=html:reports/htmlcov \
            --html=reports/test-report.html \
            --self-contained-html \
            --cov-fail-under=80

      - name: Extract coverage
        id: coverage
        run: |

          python <<EOF
          import xml.etree.ElementTree as ET
          import os

          root = ET.parse(
            "reports/coverage.xml"
          ).getroot()

          pct = round(
            float(
              root.attrib.get(
                "line-rate",
                0
              )
            ) * 100,
            2
          )

          with open(
            os.environ["GITHUB_OUTPUT"],
            "a"
          ) as f:

              f.write(
                f"pct={pct}\n"
              )

          print(pct)
          EOF

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: reports/

# ============================================================
# LINT
# ============================================================

  lint:

    name: Lint

    runs-on: ubuntu-latest

    outputs:
      status: ${{ steps.status.outputs.value }}

    steps:

      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Ruff
        run: pip install ruff

      - name: Run Ruff
        run: |

          mkdir -p reports

          ruff check . \
            > reports/lint-report.txt \
            2>&1 || true

      - name: Status
        id: status
        run: |

          if grep -q "error" reports/lint-report.txt; then

            echo "value=warning" >> $GITHUB_OUTPUT

          else

            echo "value=passed" >> $GITHUB_OUTPUT

          fi

      - uses: actions/upload-artifact@v4
        with:
          name: lint-report
          path: reports/lint-report.txt

# ============================================================
# SECURITY
# ============================================================

  security:

    name: Security Scan

    runs-on: ubuntu-latest

    outputs:
      status: ${{ steps.status.outputs.value }}

    steps:

      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Bandit
        run: pip install bandit

      - name: Run Bandit
        run: |

          mkdir -p reports

          bandit -r app \
            -f json \
            -o reports/bandit-report.json || true

      - name: Status
        id: status
        run: |

          COUNT=$(python <<EOF
          import json

          data=json.load(
            open(
              "reports/bandit-report.json"
            )
          )

          print(
            len(
              data.get(
                "results",
                []
              )
            )
          )
          EOF
          )

          if [ "$COUNT" -gt "0" ]; then

            echo "value=warning" >> $GITHUB_OUTPUT

          else

            echo "value=passed" >> $GITHUB_OUTPUT

          fi

      - uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: reports/bandit-report.json

# ============================================================
# DEPENDENCY
# ============================================================

  dependency-scan:

    name: Dependency Scan

    runs-on: ubuntu-latest

    outputs:
      vulns: ${{ steps.count.outputs.total }}

    steps:

      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install tools
        run: |
          pip install -r requirements.txt
          pip install pip-audit

      - name: Scan dependencies
        run: |

          mkdir -p reports

          pip-audit \
            -r requirements.txt \
            -f json \
            -o reports/dependency-report.json || true

      - name: Count vulns
        id: count
        run: |

          python <<EOF

          import json
          import os

          data=json.load(
            open(
              "reports/dependency-report.json"
            )
          )

          total=0

          for d in data.get(
            "dependencies",
            []
          ):

            total += len(
              d.get(
                "vulns",
                []
              )
            )

          with open(
            os.environ["GITHUB_OUTPUT"],
            "a"
          ) as f:

            f.write(
              f"total={total}\n"
            )

          print(total)

          EOF

      - uses: actions/upload-artifact@v4
        with:
          name: dependency-report
          path: reports/dependency-report.json

# ============================================================
# DASHBOARD
# ============================================================

  dashboard:

    name: Publish Dashboard

    runs-on: ubuntu-latest

    needs:
      - test
      - lint
      - security
      - dependency-scan

    permissions:
      contents: write

    steps:

      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: test-reports
          path: site/test-report

      - uses: actions/download-artifact@v4
        with:
          name: lint-report
          path: site/lint-report

      - uses: actions/download-artifact@v4
        with:
          name: security-report
          path: site/security-report

      - uses: actions/download-artifact@v4
        with:
          name: dependency-report
          path: site/dependency-report

# ============================================================
# STATUS.JSON
# ============================================================

      - name: Build status.json
        run: |

          mkdir -p site

          cat > site/status.json <<EOF
          {
            "commit":"${{ github.sha }}",
            "branch":"${{ github.ref_name }}",
            "run_id":"${{ github.run_id }}",

            "coverage":
              "${{ needs.test.outputs.coverage }}",

            "lint":
              "${{ needs.lint.outputs.status }}",

            "security":
              "${{ needs.security.outputs.status }}",

            "dependency":
              "${{ needs.dependency-scan.outputs.vulns }}"
          }
          EOF

# ============================================================
# DASHBOARD HTML
# ============================================================

      - name: Generate Dashboard
        run: |

          cat > site/index.html <<'EOF'

          <!DOCTYPE html>
          <html lang="en">

          <head>

          <meta charset="UTF-8"/>

          <meta
            name="viewport"
            content="width=device-width, initial-scale=1.0"
          />

          <title>
            FastAPI Pipeline Dashboard
          </title>

          <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

          <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap"
            rel="stylesheet"
          />

          <style>

          :root{

            --bg:#020617;
            --card:#0f172a;
            --border:#1e293b;

            --text:#f8fafc;
            --muted:#94a3b8;

            --green:#22c55e;
            --amber:#f59e0b;
            --red:#ef4444;
            --blue:#2563eb;
          }

          *{
            margin:0;
            padding:0;
            box-sizing:border-box;
          }

          body{

            font-family:'Inter',sans-serif;

            background:
            radial-gradient(circle at top right,#0ea5e920,transparent 25%),
            radial-gradient(circle at bottom left,#8b5cf620,transparent 25%),
            var(--bg);

            color:var(--text);

            overflow-x:hidden;
          }

          /* SIDEBAR */

          .sidebar{

            width:220px;

            position:fixed;

            top:0;
            left:0;
            bottom:0;

            background:#020817;

            border-right:1px solid var(--border);

            padding:18px;
          }

          .logo{

            font-size:20px;
            font-weight:900;

            margin-bottom:24px;

            color:#3b82f6;
          }

          .nav-item{

            padding:12px 14px;

            border-radius:14px;

            background:#111827;

            border:1px solid var(--border);

            font-size:14px;
            font-weight:700;

            margin-bottom:20px;
          }

          .section-title{

            color:#64748b;

            font-size:11px;

            letter-spacing:.15em;

            margin-bottom:14px;
          }

          .report{

            display:flex;
            justify-content:space-between;
            align-items:center;

            padding:12px 10px;

            border-radius:12px;

            margin-bottom:8px;

            text-decoration:none;

            color:white;

            font-size:13px;
          }

          .report:hover{
            background:#ffffff08;
          }

          .pill{

            padding:4px 9px;

            border-radius:999px;

            font-size:11px;
            font-weight:700;
          }

          .green{
            background:#14532d;
            color:#4ade80;
          }

          .blue{
            background:#172554;
            color:#60a5fa;
          }

          .amber{
            background:#78350f;
            color:#fbbf24;
          }

          .gray{
            background:#3f3f46;
            color:#facc15;
          }

          .footer{

            position:absolute;

            bottom:20px;

            left:18px;
            right:18px;

            border-top:1px solid var(--border);

            padding-top:16px;

            color:#94a3b8;

            font-size:12px;
          }

          /* MAIN */

          .main{

            margin-left:220px;

            width:calc(100vw - 220px);

            padding:18px;
          }

          .title{

            font-size:28px;
            font-weight:900;
          }

          .subtitle{

            margin-top:8px;

            color:var(--muted);

            font-size:13px;
          }

          .run{

            margin-top:14px;

            border:none;

            background:linear-gradient(
              to right,
              #2563eb,
              #3b82f6
            );

            color:white;

            padding:10px 18px;

            border-radius:12px;

            font-size:13px;
            font-weight:700;

            box-shadow:0 8px 20px #2563eb30;
          }

          /* HERO */

          .hero{

            margin-top:20px;

            background:rgba(15,23,42,.72);

            border:1px solid var(--border);

            border-radius:22px;

            padding:20px;
          }

          .health{

            display:inline-flex;
            align-items:center;
            gap:8px;

            background:#052e16;

            color:#4ade80;

            padding:8px 14px;

            border-radius:999px;

            font-size:13px;
            font-weight:700;
          }

          .hero-text{

            margin-top:18px;

            color:#cbd5e1;

            line-height:1.7;

            font-size:14px;
          }

          /* PIPELINE */

          .pipeline{

            margin-top:24px;

            display:flex;
            align-items:center;

            gap:12px;

            overflow-x:auto;

            padding-bottom:10px;
          }

          .stage{

            min-width:130px;

            background:#0b1220;

            border:1px solid #1e293b;

            border-radius:18px;

            padding:16px;

            text-align:center;
          }

          .stage.success{
            border-color:#14532d;
          }

          .stage.warning{
            border-color:#78350f;
          }

          .icon{

            width:42px;
            height:42px;

            border-radius:50%;

            display:flex;
            align-items:center;
            justify-content:center;

            margin:auto;

            font-size:18px;
            font-weight:900;
          }

          .success .icon{
            background:#14532d;
            color:#4ade80;
          }

          .warning .icon{
            background:#78350f;
            color:#fbbf24;
          }

          .stage-name{

            margin-top:12px;

            font-size:16px;
            font-weight:800;
          }

          .stage-status{

            margin-top:6px;

            font-size:13px;
          }

          .stage-time{

            margin-top:6px;

            color:#94a3b8;

            font-size:11px;
          }

          .connector{

            min-width:40px;

            display:flex;
            justify-content:center;

            gap:5px;
          }

          .connector div{

            width:9px;
            height:9px;

            border-radius:50%;

            background:#22c55e;
          }

          .connector.warn div{
            background:#fbbf24;
          }

          /* METRICS */

          .metrics{

            margin-top:18px;

            display:grid;

            grid-template-columns:
            repeat(5,1fr);

            gap:12px;
          }

          .metric{

            background:rgba(15,23,42,.72);

            border:1px solid var(--border);

            border-radius:18px;

            padding:16px;
          }

          .metric-label{

            color:#94a3b8;

            font-size:11px;
          }

          .metric-value{

            margin-top:12px;

            font-size:30px;
            font-weight:900;
          }

          .metric-sub{

            margin-top:8px;

            color:#cbd5e1;

            font-size:12px;
          }

          .metric-progress{

            margin-top:12px;

            height:6px;

            background:#1e293b;

            border-radius:999px;

            overflow:hidden;
          }

          .metric-fill{

            height:100%;

            width:80%;

            background:linear-gradient(
              to right,
              #22c55e,
              #4ade80
            );
          }

          /* BOTTOM */

          .bottom{

            margin-top:18px;

            display:grid;

            grid-template-columns:
            1.2fr 1fr 1fr;

            gap:16px;
          }

          .panel{

            background:rgba(15,23,42,.72);

            border:1px solid var(--border);

            border-radius:20px;

            padding:18px;
          }

          .panel-title{

            font-size:20px;
            font-weight:800;

            margin-bottom:18px;
          }

          canvas{
            max-width:100% !important;
          }

          /* TIMELINE */

          .timeline{
            display:flex;
            flex-direction:column;
            gap:18px;
          }

          .event{
            display:flex;
            gap:12px;
          }

          .event-dot{

            width:12px;
            height:12px;

            border-radius:50%;

            margin-top:4px;

            flex-shrink:0;
          }

          .event-time{

            color:#94a3b8;

            font-size:11px;

            margin-bottom:4px;
          }

          .event-title{

            font-size:15px;
            font-weight:700;
          }

          .event-desc{

            margin-top:4px;

            color:#cbd5e1;

            font-size:12px;

            line-height:1.5;
          }

          /* FOOT */

          .foot{

            margin-top:18px;

            text-align:center;

            color:#94a3b8;

            font-size:12px;
          }

          /* RESPONSIVE */

          @media(max-width:1400px){

            .metrics{
              grid-template-columns:repeat(2,1fr);
            }

            .bottom{
              grid-template-columns:1fr;
            }
          }

          @media(max-width:1100px){

            .sidebar{
              position:relative;
              width:100%;
              height:auto;
            }

            .main{
              margin-left:0;
              width:100%;
            }

            .footer{
              position:relative;
              left:0;
              right:0;
              bottom:0;
              margin-top:20px;
            }
          }

          @media(max-width:700px){

            .metrics{
              grid-template-columns:1fr;
            }

            .pipeline{
              flex-direction:column;
              align-items:stretch;
            }

            .connector{
              display:none;
            }
          }

          </style>

          </head>

          <body>

          <!-- SIDEBAR -->

          <div class="sidebar">

            <div class="logo">
              🚀 FastAPI
            </div>

            <div class="nav-item">
              📊 Overview
            </div>

            <div class="section-title">
              REPORTS
            </div>

            <a
              class="report"
              href="./test-report/test-report.html"
              target="_blank"
            >
              <span>✅ Test Results</span>
              <div class="pill green">
                pass
              </div>
            </a>

            <a
              class="report"
              href="./test-report/htmlcov/index.html"
              target="_blank"
            >
              <span>📈 Coverage</span>
              <div class="pill blue">
                80%
              </div>
            </a>

            <a
              class="report"
              href="./lint-report/lint-report.txt"
              target="_blank"
            >
              <span>📄 Lint</span>
              <div class="pill amber">
                warn
              </div>
            </a>

            <a
              class="report"
              href="./security-report/bandit-report.json"
              target="_blank"
            >
              <span>🛡 Security</span>
              <div class="pill amber">
                warn
              </div>
            </a>

            <a
              class="report"
              href="./dependency-report/dependency-report.json"
              target="_blank"
            >
              <span>📦 Dependency</span>
              <div class="pill gray">
                35
              </div>
            </a>

            <div class="footer">

              <div>
                Generated 12 May 2026
              </div>

              <div style="margin-top:8px;">
                FastAPI Enterprise Dashboard
              </div>

            </div>

          </div>

          <!-- MAIN -->

          <div class="main">

            <div class="title">
              FastAPI pipeline dashboard
            </div>

            <div class="subtitle">
              Commit 2833df5 • main • 11 May 2026
            </div>

            <button class="run">
              ▶ Run #290730
            </button>

            <!-- HERO -->

            <div class="hero">

              <div class="health">
                🟢 Pipeline Healthy
              </div>

              <div class="hero-text">
                Real-time CI/CD visibility with testing,
                coverage analysis, security scanning,
                dependency monitoring and deployment insights.
              </div>

              <!-- PIPELINE -->

              <div class="pipeline">

                <div class="stage success">

                  <div class="icon">
                    ✓
                  </div>

                  <div class="stage-name">
                    Checkout
                  </div>

                  <div class="stage-status">
                    Completed
                  </div>

                  <div class="stage-time">
                    11:05
                  </div>

                </div>

                <div class="connector">
                  <div></div>
                  <div></div>
                  <div></div>
                </div>

                <div class="stage success">

                  <div class="icon">
                    ✓
                  </div>

                  <div class="stage-name">
                    Tests
                  </div>

                  <div class="stage-status">
                    Passed
                  </div>

                  <div class="stage-time">
                    11:06
                  </div>

                </div>

                <div class="connector">
                  <div></div>
                  <div></div>
                  <div></div>
                </div>

                <div class="stage success">

                  <div class="icon">
                    ✓
                  </div>

                  <div class="stage-name">
                    Coverage
                  </div>

                  <div class="stage-status">
                    80.4%
                  </div>

                  <div class="stage-time">
                    11:06
                  </div>

                </div>

                <div class="connector warn">
                  <div></div>
                  <div></div>
                  <div></div>
                </div>

                <div class="stage warning">

                  <div class="icon">
                    ⚠
                  </div>

                  <div class="stage-name">
                    Lint
                  </div>

                  <div class="stage-status">
                    Warning
                  </div>

                  <div class="stage-time">
                    11:07
                  </div>

                </div>

                <div class="connector warn">
                  <div></div>
                  <div></div>
                  <div></div>
                </div>

                <div class="stage warning">

                  <div class="icon">
                    ⚠
                  </div>

                  <div class="stage-name">
                    Security
                  </div>

                  <div class="stage-status">
                    Warning
                  </div>

                  <div class="stage-time">
                    11:08
                  </div>

                </div>

              </div>

            </div>

            <!-- METRICS -->

            <div class="metrics">

              <div class="metric">

                <div class="metric-label">
                  TESTS
                </div>

                <div class="metric-value">
                  Passed
                </div>

                <div class="metric-sub">
                  All tests passed
                </div>

              </div>

              <div class="metric">

                <div class="metric-label">
                  COVERAGE
                </div>

                <div class="metric-value">
                  80.4%
                </div>

                <div class="metric-progress">
                  <div class="metric-fill"></div>
                </div>

              </div>

              <div class="metric">

                <div class="metric-label">
                  LINT
                </div>

                <div class="metric-value">
                  Warning
                </div>

                <div class="metric-sub">
                  Issues detected
                </div>

              </div>

              <div class="metric">

                <div class="metric-label">
                  SECURITY
                </div>

                <div class="metric-value">
                  Warning
                </div>

                <div class="metric-sub">
                  Review report
                </div>

              </div>

              <div class="metric">

                <div class="metric-label">
                  VULNS
                </div>

                <div class="metric-value">
                  35
                </div>

                <div class="metric-sub">
                  Total vulnerabilities
                </div>

              </div>

            </div>

            <!-- BOTTOM -->

            <div class="bottom">

              <!-- COVERAGE -->

              <div class="panel">

                <div class="panel-title">
                  Coverage Trend
                </div>

                <canvas
                  id="coverageChart"
                  height="150"
                ></canvas>

              </div>

              <!-- DONUT -->

              <div class="panel">

                <div class="panel-title">
                  Vulnerabilities
                </div>

                <canvas
                  id="vulnChart"
                  height="150"
                ></canvas>

              </div>

              <!-- ACTIVITY -->

              <div class="panel">

                <div class="panel-title">
                  Recent Activity
                </div>

                <div class="timeline">

                  <div class="event">

                    <div
                      class="event-dot"
                      style="background:#22c55e"
                    ></div>

                    <div>

                      <div class="event-time">
                        11:05
                      </div>

                      <div class="event-title">
                        Pipeline started
                      </div>

                      <div class="event-desc">
                        GitHub workflow triggered
                      </div>

                    </div>

                  </div>

                  <div class="event">

                    <div
                      class="event-dot"
                      style="background:#22c55e"
                    ></div>

                    <div>

                      <div class="event-time">
                        11:06
                      </div>

                      <div class="event-title">
                        Tests passed
                      </div>

                      <div class="event-desc">
                        Coverage generated
                      </div>

                    </div>

                  </div>

                  <div class="event">

                    <div
                      class="event-dot"
                      style="background:#f59e0b"
                    ></div>

                    <div>

                      <div class="event-time">
                        11:07
                      </div>

                      <div class="event-title">
                        Lint warning
                      </div>

                      <div class="event-desc">
                        Check lint report
                      </div>

                    </div>

                  </div>

                </div>

              </div>

            </div>

            <div class="foot">
              FastAPI Enterprise CI/CD Dashboard
            </div>

          </div>

          <script>

          new Chart(
            document.getElementById(
              'coverageChart'
            ),
            {

              type:'line',

              data:{

                labels:[
                  '05 May',
                  '06 May',
                  '07 May',
                  '08 May',
                  '09 May',
                  '10 May',
                  '11 May'
                ],

                datasets:[{

                  data:[
                    73,
                    75,
                    77,
                    75,
                    82,
                    89,
                    81
                  ],

                  borderColor:'#22c55e',

                  backgroundColor:
                  'rgba(34,197,94,.15)',

                  fill:true,

                  tension:.4
                }]
              },

              options:{

                plugins:{
                  legend:{display:false}
                },

                scales:{

                  x:{
                    ticks:{
                      color:'#94a3b8',
                      font:{size:10}
                    },

                    grid:{
                      color:'#1e293b'
                    }
                  },

                  y:{
                    ticks:{
                      color:'#94a3b8',
                      font:{size:10}
                    },

                    grid:{
                      color:'#1e293b'
                    }
                  }
                }
              }
            }
          );

          new Chart(
            document.getElementById(
              'vulnChart'
            ),
            {

              type:'doughnut',

              data:{

                labels:[
                  'Critical',
                  'High',
                  'Medium',
                  'Low'
                ],

                datasets:[{

                  data:[
                    4,
                    12,
                    28,
                    42
                  ],

                  backgroundColor:[
                    '#ef4444',
                    '#f97316',
                    '#fbbf24',
                    '#2563eb'
                  ],

                  borderWidth:0
                }]
              },

              options:{

                cutout:'72%',

                plugins:{

                  legend:{

                    position:'bottom',

                    labels:{
                      color:'#f8fafc',
                      boxWidth:10,
                      font:{size:10}
                    }
                  }
                }
              }
            }
          );

          </script>

          </body>
          </html>

          EOF

# ============================================================
# NO JEKYLL
# ============================================================

      - name: Disable Jekyll
        run: touch site/.nojekyll

# ============================================================
# DEPLOY
# ============================================================

      - name: Deploy GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
          force_orphan: true